fun void fib(int n) {
	int n1 = 0, n2 = 1, n3;
	if (n == 1) {
		print("0\n");
	} else if (n == 2) {
		print("0 1\n");
	} else {
		print("0 1 ");
		for (int i : (0, n - 2, 1)) {
			n3 = n1 + n2;
			if (i == n - 3) {
				print(n3);
				print("\n");
			} else {
				print(n3);
				print(" ");
			}
			n1 = n2;
			n2 = n3;
		}
	}
}

fun int main() {
	int n;
	read(n);
	fib(n);
	return 0;
}